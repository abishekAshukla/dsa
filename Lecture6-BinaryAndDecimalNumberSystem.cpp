#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    // trying to convert decimal into binary , error coming
    // int n;
    // cin>>n;

    // int ans = 0;
    // int i = 0;

    // while (n != 0)
    // {
    //     int bit = n & 1;

    //     ans = (bit * pow(10,i)) + ans;

    //     n = n >> 1;
    //     i++;
    // }

    // cout<<"Answer is "<<ans<<endl;

    // Convert binary into decimal

    // int n;
    // cin >> n;

    // int answer = 0;
    // int count = 0;

    // for (int i = n; n > 0; count++) // this loop will run number of digits in n times, ex: it will run 5 times for 12365
    // {
    //     int lastDigit = n % 10;
    //     n = n / 10;
    //     if (lastDigit == 0)
    //     {
    //         continue;
    //     }

    //     int power = pow(2, count);
    //     answer += power;
    // }

    // cout << answer << endl;


    return 0;
}
